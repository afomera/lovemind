<nav class="navbar navbar-default bg-light-purple">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%#= image_tag 'logo.png', class: 'logo pull-left' %>
      <%= link_to "Lovemind", root_path, class: "navbar-brand" %>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <div class="navbar-form navbar-right">
        <%= search_form_for(@search, url:"/search", :remote => true)  do |f| %>
          <%= text_field_tag :location, params[:location], placeholder: "Houston, TX", class: "form-control" %>
        <% end %>
      </div>
      <ul class="nav navbar-nav navbar-left">

        <li>
          <%= link_to new_resource_path do %>
            <%= fa_icon "hospital-o", text: "Add Resource" %>
          <% end %>
        </li>
      <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= fa_icon "home", class: "fa-dark", text: "#{current_user.username}" %> <%= fa_icon "caret-down", class: 'fa-dark' %></a>
          <ul class="dropdown-menu">
            <li>
            <%= link_to edit_user_registration_path do %>
              <%= fa_icon "cog", text: 'Settings' %>
            <% end %>
          </li>
          <li>
          <%= link_to destroy_user_session_path, method: :delete do %>
              <%= fa_icon "sign-out", text: 'Logout' %>
            <% end %>
          </li>
          </ul>
        </li>
      <% else %>
        <li>
          <%= link_to new_user_session_path do %>
            <%= fa_icon "sign-in", text: 'Login' %>
          <% end %>
        </li>
        <li>
          <%= link_to new_user_registration_path do %>
            <%= fa_icon "user-plus", text: 'Sign Up' %>
          <% end %>
        </li>
      <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>
